spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/clubmanagementsystem
    username: myuser
    password: mypassword
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  flyway:
    baseline-on-migrate: true
    locations: classpath:db/migration
